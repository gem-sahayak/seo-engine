# Custom Audit Plugin Template

This is an ASTRA Read-Only Plugin Template.

## Usage
1. Inspect `plugin.json` for permissions and hooks.
2. Implement your hook logic in `index.js`.
3. Test using `node cli.js sdk:test`.
