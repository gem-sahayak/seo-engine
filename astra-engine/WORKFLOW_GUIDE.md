# ASTRA Workflow Intelligence Guide (v1.9.0)

Welcome to **ASTRA Autonomous Workflow Intelligence Engine**, the orchestration layer for ASTRA Engine.

## CLI Commands
```bash
# Run Workflow Intelligence Engine
node cli.js workflow

# Specific Options
node cli.js workflow --run
node cli.js workflow --history
node cli.js workflow --metrics
node cli.js workflow --rules
node cli.js workflow --recommend
node cli.js workflow --events
node cli.js workflow --queue
node cli.js workflow --schedule
```
